
DJANGO_SECRET_KEY=changeme
DEBUG=True

# Email (for dev you can use console backend; in settings set EMAIL_BACKEND to console if needed)
EMAIL_USER=example@example.com
EMAIL_PASSWORD=changeme
SMTP_SERVER=smtp.example.com
SMTP_PORT=587

# SMS
SMS_LOGIN=your_sms_login
SMS_PASSWORD=your_sms_password

# 1C API (dummy for dev)
ONEC_API_TOKEN=devtoken
ONEC_API_URL=https://example.invalid/onec/debts
